[package]
license = "Apache-2.0"
name = "shell-terminal"
version = "0.1.0"
edition.workspace = true
description = "Local, ACP, and PTY terminal runners extracted from xvora-shell so they compile in parallel."

[dependencies]
agent-client-protocol = { workspace = true }
async-trait = { workspace = true }
base64 = { workspace = true }
chrono = { workspace = true }
futures = { workspace = true }
portable-pty = "0.9"
process-wrap = { version = "9.0.0", features = ["tokio1", "job-object", "creation-flags"] }
serde = { workspace = true }
serde_json = { workspace = true }
shlex = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true, features = [
    "fs",
    "io-util",
    "macros",
    "process",
    "sync",
    "time",
] }
tracing = { workspace = true }
uuid = { workspace = true, features = ["v7"] }
acp-lib = { workspace = true, package = "xvora-acp-lib" }
xvora-config = { workspace = true }
xvora-paths = { path = "../xvora-paths" }
xvora-sandbox = { path = "../xvora-sandbox", default-features = false }
xvora-tools = { workspace = true }
xvora-workspace = { workspace = true }
tty-utils = { workspace = true, package = "xvora-tty-utils" }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
